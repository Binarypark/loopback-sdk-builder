/* tslint:disable */
<% for (var modelName in models) {
  var meta = models[modelName];
  // transient models are not pulled from REST and are untampered models which still have dataSource
  if(meta.dataSource == null) {
    // capitalize the model name
    modelName = modelName[0].toUpperCase() + modelName.slice(1);
-%>
export * from './<%- kebabalize(modelName) %>';
<% }} // for modelName in models -%>
export * from './sdk-models';
export * from './logger.service';
